<h1>All Dogs:</h1>

<% new_list = @dogs.sort_by {|dog| dog.employees.all.count} %>

<% new_list.each do |dog| %>


<%= link_to dog.name, dog_path(dog) %>
<br>
<br>

<% end %>

#Sort the dogs on the index page by the number
of employees that belong to them